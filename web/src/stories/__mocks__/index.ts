/**
 * Centralized Mock Data for Storybook
 *
 * This file contains all mock data used across Storybook stories.
 * Keeping mock data centralized ensures consistency and makes it easier to maintain.
 */

// Menu mocks
export { mockMenus, mockCategories, mockMenuItems, singleMenu, emptyMenus } from './mockMenus'

// Website mocks
export { mockWebsites, mockWebsitePages, singleWebsite, emptyWebsites } from './mockWebsites'

// Location mocks
export { mockLocations, singleLocation, emptyLocations } from './mockLocations'

// Restaurant mocks
export { mockUserRestaurants, singleRestaurant, emptyRestaurants } from './mockRestaurants'
